# Inkwell

Beta! This is a work in progress. Not enabled for Micro.blog accounts yet.

## Local development

Open the app over HTTP so module scripts can load without CORS errors.

```sh
cd static
python3 -m http.server 8000
```

Then visit `http://localhost:8000` in your browser.
